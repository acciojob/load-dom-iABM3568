// Wait for DOM to be fully loaded
document.addEventListener('DOMContentLoaded', function() {
  // Write text directly to body without creating child elements
  document.body.textContent = 'DOM load success';
});